import { Scenario } from '../scenario'

const testScenario : Scenario = {
  weeks: [{
    army: [
      'soldier',
      'soldier',
      'soldier',
      'soldier',
      'soldier',
      'soldier',
      'soldier',
      'soldier',
    ]
  }]
}

export { testScenario }